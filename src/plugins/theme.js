import { createTheme } from '@material-ui/core/styles'

const theme = createTheme({
    typography: {
        fontSize: "14px"
    },
    palette: {
        primary: {
            main: "#ED4D77",
        },
        secondary: {
            main: "#ffffff",
        },
    },
})

export default theme
